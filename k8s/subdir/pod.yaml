apiVersion: v1
kind: Pod
metadata:
  name: nginx
spec:
  securityContext:
    runAsUser: 1000
  containers:
  - name: nginx
    securityContext:
      allowPrivilegeEscalation: false
    image: nginx:1.23.4
    ports:
    - containerPort: 80
